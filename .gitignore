# Build artifacts (downloaded core, assembled pak)
build/

# Binaries (not version-controlled — re-fetch via deploy_phase_a.sh)
_roms/
artwork/*.zip

# Empty placeholder dirs
dist/

# Phase B work area
tmp/

# Phase C (native MAME) work areas
mame_build/mame_src/
mame_build/out/
mame_build/*.log
mame_build/joydump
mame_build/present_test

# IDE
.idea/

# OS
.DS_Store
